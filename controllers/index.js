const userController = require('./userController')
const authController = require('./authController')
const restrictedController = require('./restrictedController')

module.exports = {
  userController,
  authController,
  restrictedController
}
