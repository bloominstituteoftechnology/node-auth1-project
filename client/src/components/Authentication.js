import React from 'react';

const Authenticate = App => {
  class InnerApp extends React.Component {
    constructor(props) {
      super(props);
      this.state = {
        loggedIn: false
      }
    }
  }
}
