import React from 'react';
import { NavLink } from 'react-router-dom'

const Home = props => {
        return (
            <div>
                <NavLink to="/register"> Register </NavLink>
            </div>
        )
    }

export default Home