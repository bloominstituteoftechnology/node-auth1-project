import React from 'react';

function User(props) {
  return (
    <div>
      <h1>Hi</h1>
      {/* <p>{props.user.username}</p>
      <p>{props.user.password}</p> */}
    </div>
  );
}

export default User;
